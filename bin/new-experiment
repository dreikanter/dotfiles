#!/bin/bash

if [ $# -eq 0 ]; then
  echo "Usage: new-experiment <experiment-name>"
  exit 1
fi

# Get slug from Ruby script
slug=$($HOME/.dotfiles/bin/new-experiment-path "$@")
if [ $? -ne 0 ]; then
  exit 1
fi

dir="$HOME/src/$slug"

# Create directory and cd into it
mkdir -p "$dir"
cd "$dir"