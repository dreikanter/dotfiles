#!/usr/bin/env bash

set -euo pipefail

NOTES="${GOPATH:-$HOME/go}/bin/notes"
file_path="$("$NOTES" resolve --type todo)"
${NOTES_EDITOR:-/opt/homebrew/bin/subl --add} "$(dirname "$(dirname "$(dirname "$file_path")")")" "$file_path" &
disown
