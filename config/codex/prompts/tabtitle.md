---
description: Refresh tab title
---

Use the command `wezterm cli set-tab-title "Title"` to rename current WezTerm tab you're running in, so it reflect the subject of the current session. Keep the tab title compact.
